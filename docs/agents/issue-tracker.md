# Issue tracker

Issues for agent skills live as local markdown under `.scratch/<feature-slug>/issues/`.

Triage status is recorded in each ticket file as `**Status:** ready-for-agent` (or other triage roles when used).
